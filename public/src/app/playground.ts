export class Playground {
    background: number;
    width: number;
    height: number;

    constructor(background, width, height) {
        this.background = background;
        this.width = width;
        this.height = height;
    }
}
